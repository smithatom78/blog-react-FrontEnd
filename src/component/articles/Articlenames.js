import React from 'react';
import { Link, useParams } from 'react-router-dom';
export const Articlenames = (props) => {
    const {name,title,setarticleData,username} = props;
  return <div>
         
                    <h4>hi{title}</h4>
           
  </div>;
};
